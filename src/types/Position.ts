/**
 * 2D position coordinates for moving objects
 * Represents absolute positioning on the screen
 */
export interface Position {
  /** Top offset from viewport top in pixels */
  top: number;
  /** Left offset from viewport left in pixels */
  left: number;
}
